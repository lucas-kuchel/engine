{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake -B build --preset release && cmake --build build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}